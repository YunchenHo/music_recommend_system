### ===========================================
### Music Recommend System — API 測試
### ===========================================
### 使用方式：VS Code 安裝 REST Client 套件
### 按每個請求上方的 "Send Request" 即可執行
###
### 測試流程：
### 1. 先執行 dev-login 取得 session
### 2. 從 response header Set-Cookie 拿到 sessionid 和 csrftoken
### 3. 填入下方的變數
### 4. 依序測試各 API
### ===========================================

### 🔧 變數設定（從 dev-login 回傳的 Set-Cookie 取得後填入）
@host = http://localhost:8000
@sessionid = 7abcw44vq7daa73zvuseemalfghkgv2q
@csrftoken = QtcOkWFSJ3cSes2ybrP6vejkRLDqMpeN
### -------------------------------------------
### 1. 模擬登入（Dev Only, DEBUG=True）
### 執行後從 Response Headers 的 Set-Cookie 取得 sessionid 和 csrftoken
### -------------------------------------------
POST {{host}}/api/auth/dev-login
Content-Type: application/json

{
  "username": "testuser"
}

### -------------------------------------------
### 2. 取得推薦歌曲（Placeholder: 隨機 9 首）
### -------------------------------------------
GET {{host}}/api/songs/recommendations
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 3. 取得收藏列表（應該是空的）
### -------------------------------------------
GET {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 4. 收藏一首歌（焚情 - 張信哲）
### -------------------------------------------
POST {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}
Content-Type: application/json

{
  "song_id": 523020
}

### -------------------------------------------
### 5. 再收藏一首歌（PLAYING WITH FIRE - BLACKPINK）
### -------------------------------------------
POST {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}
Content-Type: application/json

{
  "song_id": 1867000
}

### -------------------------------------------
### 6. 取得收藏列表（應該有 2 首）
### -------------------------------------------
GET {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 7. 重複收藏同一首歌（應該回 400）
### -------------------------------------------
POST {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}
Content-Type: application/json

{
  "song_id": 523020
}

### -------------------------------------------
### 8. 收藏不存在的歌（應該回 404）
### -------------------------------------------
POST {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}
Content-Type: application/json

{
  "song_id": 99999999
}

### -------------------------------------------
### 9. 取消收藏（焚情）
### -------------------------------------------
DELETE {{host}}/api/songs/favorites/523020
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}

### -------------------------------------------
### 10. 取得收藏列表（應該剩 1 首）
### -------------------------------------------
GET {{host}}/api/songs/favorites
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 11. 取消收藏不存在的歌（應該回 404）
### -------------------------------------------
DELETE {{host}}/api/songs/favorites/99999999
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}
X-CSRFToken: {{csrftoken}}

### -------------------------------------------
### 12. 取得單首歌曲詳細資訊（含收藏狀態）
### -------------------------------------------
GET {{host}}/api/songs/523020
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 13. 取得不存在的歌曲（應該回 404）
### -------------------------------------------
GET {{host}}/api/songs/99999999
Cookie: sessionid={{sessionid}}; csrftoken={{csrftoken}}

### -------------------------------------------
### 14. 未登入測試（不帶 cookie，應該回 403）
### -------------------------------------------
GET {{host}}/api/songs/recommendations
